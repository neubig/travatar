#include <travatar/tuner-greedy-mert.h>

using namespace std;
using namespace travatar;

SparseMap TunerGreedyMert::Tune(
               const vector<vector<pair<SparseMap, double> > > & examps,
               const SparseMap & init = SparseMap()) {
    SparseMap ret;
    THROW_ERROR("Not implemented yet");
    return ret;
}
